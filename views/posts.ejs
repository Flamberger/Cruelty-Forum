<!doctype html>
<html>
  <head>
    <title>Primordial Discussions</title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>Communication is the seat of the Soul</h1>
    <h1>Human thought collective:</h1>
    <ul>
        <% for(i=0; i<posts.length; i++){ %>
            <h2><%= posts[i].title %></h2>
            <% for(j=0; j<posts.length; j++){ %>
                <% if (posts[j].title == posts[i].title) {%>
                    <p><b><%= posts[j].header %></b>, by <%= posts[j].username %>:</p>
                    <li><%= posts[j].body %></li>
                <% } %>
            <% } %>
            </br>
        <% } %>
    </ul>
  </body>
</html>